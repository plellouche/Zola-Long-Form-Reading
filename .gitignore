# --- Env / secrets ---
.env
.env.local
.env.*.local
!.env.example

# --- Node / pnpm ---
node_modules/
.pnpm-store/
.pnpm-debug.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Next.js ---
.next/
out/
.vercel/
next-env.d.ts

# --- TypeScript / build output ---
*.tsbuildinfo
dist/
build/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Alembic ---
# (keep migration scripts; ignore any local SQLite scratch DBs)
*.db
*.sqlite
*.sqlite3

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# --- Claude Code local state ---
.claude/settings.local.json

# --- Coverage / logs ---
coverage/
*.log

# --- Finder/iCloud conflict copies ---
# macOS sometimes creates "<name> 2.ext" duplicates of files that get
# touched from multiple processes (Finder + editor) or in iCloud sync.
# They aren't valid Next route segments (spaces in names) and silently
# bloat the deploy. See HEAD~ for the day this caught us.
* 2.tsx
* 2.ts
* 2.woff2
* 2.svg
* 2.css
