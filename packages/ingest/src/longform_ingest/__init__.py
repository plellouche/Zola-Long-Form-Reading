"""Longform ingestion pipeline.

Populated in Phase 3:
    - rss.py — RSS feed parsing + normalization
    - og_fetcher.py — OpenGraph metadata fetch for submitted URLs
    - robots.py — robots.txt compliance check before any crawl
"""
